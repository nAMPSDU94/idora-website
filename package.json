{
  "name": "idora-website",
  "version": "1.0.0",
  "description": "IDORA Personalized Magazines",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "build": "cd backend && npm install && npx prisma generate"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
